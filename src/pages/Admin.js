import React from "react";
import styled from 'styled-components';

const Wrapper = styled.article`
  padding-top: 100px;
  text-align: left;
`;

//const Admin = () => (
export default () =>
  <Wrapper>
    <h2>Admin</h2>
  </Wrapper>;